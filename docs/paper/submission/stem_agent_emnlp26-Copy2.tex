\documentclass[11pt]{article}

\usepackage[review]{acl}

\usepackage{times}
\usepackage{latexsym}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{inconsolata}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}

\title{STEM Agent: A Self-Adapting, Tool-Enabled, Extensible Architecture\\
for Multi-Protocol AI Agent Systems in Production}

\author{Anonymous Submission}

\begin{document}
\maketitle

\begin{abstract}
Production AI agent systems increasingly suffer from \emph{architectural lock-in}: early commitment to a single interaction protocol, a fixed tool-integration path, and a static user model makes deployed agents hard to retarget when requirements or standards shift. We describe \textbf{STEM Agent} (Self-adapting, Tool-enabled, Extensible, Multi-agent), an architecture that unifies five interoperability protocols---A2A, AG-UI, A2UI, UCP, and AP2---behind a single gateway, isolates domain knowledge behind the Model Context Protocol (MCP), and continuously adapts per-user behavior through a Caller Profiler across 21 dimensions. Recurring interaction patterns \emph{crystallize} into reusable skills through a maturation lifecycle. We report operational lessons from six months of deployment serving 200 weekly users, including scalability patterns (horizontal scaling via externalized state), security integration (pluggable IAM with JWT/OAuth2/SAML/API-Key), and observability (OpenTelemetry tracing, structured logging, Prometheus metrics).
\end{abstract}

\section{Introduction}
\label{sec:intro}

Teams deploying LLM-based agents in enterprises discover that the hard problems are not which reasoning prompt to use, but how the agent connects to the business: which protocol it speaks to other agents, which commerce standard it satisfies, how it streams progress to web UIs, and how it remembers what it learned. Most agent frameworks~\citep{tran2025multiagent, ferrag2025agents} commit early to one answer for each, producing agents hard to retarget when new consumers appear. \citet{orogat2026mafbench} confirm that framework choice alone can drive $100\times$ latency differences. The interoperability landscape is fragmenting~\citep{ehtesham2025survey, li2025gluetoprotocols}: MCP has emerged as a \emph{de facto} tool protocol, A2A for agent-to-agent delegation, with additional standards for UI streaming, commerce, and payments entering production.

This paper reports on STEM Agent, a system built to serve these needs without repeatedly rewriting core logic. The design principle is biological: an undifferentiated agent core \emph{differentiates} into specialized protocol handlers, tool bindings, and memory subsystems. Every external boundary---protocols, tools, user models, domains---is a pluggable differentiation point rather than a hard-wired choice.

\paragraph{Contributions.} (i)~A gateway exposing five protocols (A2A, AG-UI, A2UI, UCP, AP2), with UCP and AP2 proposed to fill commerce gaps; (ii)~a Caller Profiler learning preferences across 21 dimensions; (iii)~an MCP-native tool layer separating domain knowledge from meta-reasoning; (iv)~a skills subsystem where patterns crystallize into reusable skills; (v)~operational integration patterns---pluggable IAM, OpenTelemetry observability, horizontal-scaling architecture; (vi)~deployment experience from six months of production traffic.

This is an industry-track paper: novelty is in integration and deployment discipline, not in a new model or algorithm.

\section{Related Work}
\label{sec:related}

\paragraph{Multi-agent frameworks.}
Surveys~\citep{tran2025multiagent, ferrag2025agents} document frameworks---AutoGen~\citep{wu2023autogen}, MetaGPT~\citep{hong2023metagpt}, CAMEL~\citep{li2023camel}, CrewAI~\citep{crewai2023}, LangChain~\citep{chase2022langchain}, LangGraph~\citep{langgraph2024}---each optimizing for one coordination pattern and typically one protocol. Unlike frameworks binding each agent to a single endpoint, every STEM gateway mounts five handlers behind one middleware stack.

\paragraph{Agent protocols.}
\citet{ehtesham2025survey} compare MCP, ACP, A2A, and ANP; \citet{li2025gluetoprotocols} identify schema translation as the hard part of A2A/MCP integration. Security has received attention~\citep{habler2025a2a, anbiaee2026security}. We are not aware of a deployed system unifying these five protocols.

\paragraph{MCP and reasoning.}
Benchmarks~\citep{luo2025mcpuniverse, fan2025mcptoolbench} show LLMs struggle on real MCP servers; \citet{hasan2026mcpsmells} find 97\% of tool descriptions have quality issues. STEM uses ReAct~\citep{yao2023react}, Reflexion~\citep{shinn2023reflexion}, Debate~\citep{du2023debate}, and CoT~\citep{wei2022chain} as interchangeable strategies. For memory, we build on \citet{ai2025memorybench, gallego2026memoryastool, yadav2026synapse}.

\section{Architecture Overview}
\label{sec:architecture}

STEM Agent is organized into layers (Table~\ref{tab:layers}, Figure~\ref{fig:architecture}). Layer~3 (Agent Core) is undifferentiated; Layers~2 and 5 provide external surfaces; Layer~4 supplies persistent state. The Differentiation Layer bundles artifacts to transform the generic agent into domain variants.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{architecture.pdf}
  \caption{STEM Agent architecture. The gateway mounts five protocol handlers sharing a middleware stack. The Agent Core consumes MCP tools and a four-type memory system.}
  \label{fig:architecture}
\end{figure}

\begin{table}[t]
  \small
  \centering
  \begin{tabular}{clc}
    \toprule
    Layer & Component & Count \\
    \midrule
    1 & Caller Layer & 4 adapters \\
    2 & Gateway & 5 protocols \\
    3 & Agent Core & 5 engines \\
    3.5 & Security & 4 IAM plugins \\
    4 & Memory & 4 types \\
    5 & MCP Integration & 2 transports \\
    $\perp$ & Differentiation & personas/skills \\
    \bottomrule
  \end{tabular}
  \caption{Layered architecture.}
  \label{tab:layers}
\end{table}

\paragraph{Cognitive pipeline.}
Each request flows through: \textsc{Perceive} (intent, complexity, entities); \textsc{Adapt} (load caller profile); \textsc{MatchSkills}; \textsc{SelectStrategy} (Table~\ref{tab:strategy}); \textsc{Plan}; \textsc{Execute} (retries, circuit breaker); \textsc{Format}; and async \textsc{Learn}.

\begin{table}[t]
  \small
  \centering
  \begin{tabular}{ll}
    \toprule
    Condition & Strategy \\
    \midrule
    Tool-requiring & ReAct \\
    Complexity $> 0.8$ & Reflexion \\
    Analysis/creative & Debate \\
    Otherwise & CoT \\
    \bottomrule
  \end{tabular}
  \caption{Strategy selector.}
  \label{tab:strategy}
\end{table}

\paragraph{Implementation.}
TypeScript monorepo with six packages, Express.js~5 gateway, uniform \texttt{createRouter()} for protocol handlers.

\section{Protocol Gateway}
\label{sec:protocols}

\begin{table*}[t]
  \small
  \centering
  \begin{tabular}{llllp{5cm}}
    \toprule
    Protocol & Endpoint & Transport & Role & Use case \\
    \midrule
    A2A  & \texttt{/a2a} & JSON-RPC 2.0 & External & Agent delegation, discovery \\
    AG-UI & \texttt{/ag-ui} & SSE & External & UI streaming events \\
    A2UI & \texttt{/a2ui/*} & SSE+REST & External & Dynamic UI composition \\
    UCP  & \texttt{/ucp/*} & REST & \textbf{Proposed} & Commerce checkout, idempotency \\
    AP2  & \texttt{/ap2/*} & REST & \textbf{Proposed} & Mandate-based payments \\
    \bottomrule
  \end{tabular}
  \caption{Protocols behind the unified gateway. UCP and AP2 are proposed herein.}
  \label{tab:protocols}
\end{table*}

A2A implements Linux Foundation v0.3.0; AG-UI streams pipeline events; A2UI exposes 16 UI primitives. UCP formalizes checkout sessions with mandatory idempotency headers---proposed because we found no commerce protocol treating the agent as primary client. AP2 implements three-phase payments with audit trails.

\paragraph{Integration lessons.} (i)~\texttt{createRouter()} uniformity reduced cross-protocol bugs to near zero. (ii)~Idempotency is the hard part---UCP/AP2 headers took more review cycles than happy-path code.

\section{Caller Profiler}
\label{sec:profiler}

STEM Agent learns a Caller Profile across four categories~\citep{park2023generative}: \emph{philosophy} (8 dimensions), \emph{principles} (4), \emph{style} (5), \emph{habits} (4)---21 total.\footnote{Dimensions: philosophy---pragmatism, risk tolerance, innovation orientation, autonomy preference, etc.; principles---correctness/speed, documentation, testing, security; style---formality, verbosity, technical depth; habits---session length, iteration tendency, peak hours, topics.}

Each dimension uses EMA with $\alpha=0.1$; confidence ramps as $\text{conf}(n) = 1 - \exp(-0.1\,n)$, reaching 0.9 at $n=20$. Ten behavior parameters (reasoning depth, exploration/exploitation, verbosity, etc.) adjust during \textsc{Adapt}.

\section{Skills and Differentiation}
\label{sec:skills}

\paragraph{Skill lifecycle.}
During \textsc{Learn}, episodes sharing action patterns become \emph{progenitor} skills. After $k_c=3$ successful activations with success $\geq 0.6$, skills become \emph{committed} (may short-circuit reasoning); after $k_m=10$, \emph{mature}. Skills below 0.3 success after $\geq 10$ activations undergo \emph{apoptosis}.

\paragraph{MCP-native tools.}
All domain capabilities are acquired through MCP at runtime. Meta-reasoning stays in code---properties of the agent, not domain.

\paragraph{Differentiation.}
Domain Persona schemas parameterize the agent; an \texttt{mcp-server} package exposes differentiated variants as MCP tools. Creating a domain variant takes minutes vs.\ days previously.

\section{Memory System}
\label{sec:memory}

Four types behind a Memory Manager: \textbf{Episodic} (PostgreSQL+pgvector); \textbf{Semantic} (knowledge triples); \textbf{Procedural} (successful strategies); \textbf{User context} (per-caller state, forget-me path). Consolidation moves episodic$\to$semantic/procedural with pruning.

\section{Deployment Experience}
\label{sec:deployment}

STEM Agent has been deployed internally for six months, serving four domain variants to $\sim$200 weekly users across three teams.

\paragraph{Context.}
Containerized on Kubernetes (2--8 replicas, HPA); PostgreSQL+pgvector for memory; Redis for idempotency cache. Average 2,400 daily requests (p50: 1.2\,s, p95: 3.8\,s, p99: 7.1\,s). 14 incidents over six months: LLM rate-limits (6), MCP timeouts (4), cache races (2). MTTR: 18 minutes.

\paragraph{Scalability.}
We externalized idempotency/audit to Redis with Lua-scripted atomic ops for horizontal scaling. The cognitive pipeline is stateless; replicas share PostgreSQL/Redis. Validated linear scaling to 500 concurrent requests before LLM rate limits.

\paragraph{Security.}
Plugin-based IAM: JWT (service-to-service), OAuth2 (SSO), SAML (legacy), API-Key (partners). TTL-cached decisions; per-caller rate limits. Blocked 47 unauthorized attempts without false positives. Auth isolation: zero cognitive-pipeline changes for auth modifications.

\paragraph{Observability.}
Three pillars: (i)~Structured JSON logs with correlation IDs propagating through all phases; (ii)~OpenTelemetry distributed tracing (latency breakdown: \textsc{Perceive} 80\,ms, \textsc{Reason} 1,100\,ms, \textsc{Execute} 200\,ms); (iii)~Prometheus metrics (request count, latency histograms, skill activation). Grafana dashboards for per-protocol traffic, errors, token consumption. Tracing diagnosed the cache race---duplicate writes under concurrent retries.

\paragraph{Test suite.}
422 tests across 40 files (Vitest), 2.86\,s runtime. Coverage: engines, protocols, memory, MCP integration, IAM middleware.

\paragraph{Integration.}
Four framework adapters (AutoGen, CrewAI, LangGraph, OpenAI SDK). A CrewAI bot migrated in $\sim$2 hours via A2A delegation.

\begin{table}[t]
  \small
  \centering
  \begin{tabular}{lcccccc}
    \toprule
    Framework & Proto & Adapt & Mem & Skill & Com & Obs \\
    \midrule
    AutoGen & 1 & $\times$ & \checkmark & $\times$ & $\times$ & $\times$ \\
    MetaGPT & 1 & $\times$ & \checkmark & $\times$ & $\times$ & $\times$ \\
    CrewAI & 1 & $\times$ & \checkmark & $\times$ & $\times$ & $\times$ \\
    LangChain & 1 & $\times$ & \checkmark & $\times$ & $\times$ & $\times$ \\
    \textbf{STEM} & \textbf{5} & \checkmark & \textbf{4} & \checkmark & \checkmark & \checkmark \\
    \bottomrule
  \end{tabular}
  \caption{Capability comparison. Proto=protocols; Adapt=per-caller adaptation; Com=commerce; Obs=observability.}
  \label{tab:comparison}
\end{table}

\paragraph{Compromises.}
(i)~Idempotency/audit are in-memory by default; distributed deployment needs external store. (ii)~EMA profiler cannot model multi-modal preferences. (iii)~Strategy selection is deterministic, not learned.

\section{Conclusion}
\label{sec:conclusion}

STEM Agent demonstrates that protocol plurality, behavioral adaptation, and emergent skills can coexist when domain knowledge is behind MCP and user modeling is decoupled from domain logic. Six months of deployment validates the architecture: pluggable IAM met enterprise auth; OpenTelemetry proved essential for debugging; horizontal scaling maintained stability. Ongoing work includes learned strategy selection, additional protocol interoperability, and formal threat modeling for UCP/AP2.

\section*{Limitations}

\textbf{No public benchmark.} We report production metrics but not task-completion scores against MAFBench/MCP-Universe.

\textbf{Single organization.} All data from one enterprise context.

\textbf{Protocol maturity.} UCP/AP2 have no external adoption or formal threat model.

\textbf{Deterministic strategy.} Hand-written rules, not learned policy.

\section*{Ethical Considerations}

The Caller Profiler stores behavioral signals. We (i)~expose a forget-me operation; (ii)~keep updates reversible via bounded EMA; (iii)~scope audit logs at request level. UCP/AP2 handle payments; we require human approval above thresholds, but these protocols lack formal security review.

\bibliography{references}

\end{document}
