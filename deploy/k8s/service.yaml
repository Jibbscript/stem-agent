apiVersion: v1
kind: Service
metadata:
  name: stem-agent
  labels:
    app.kubernetes.io/name: stem-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: stem-agent
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
